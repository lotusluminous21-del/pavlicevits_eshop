/**
 * Real project photography — Mode C / Mode D per
 * 02_VISUAL_DIRECTION.md §4.1 (application moments and finished
 * results, documentary register, on-site, no posed smiles).
 */
export const projectAssets = {
  hellenicCoastVan: "/brand/projects/hellenic-coast-van.webp",
} as const;

export type ProjectAsset = keyof typeof projectAssets;
